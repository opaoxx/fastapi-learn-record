# Database lessons introduced

Lessons 0007 and 0008 introduce SQLite, SQLModel, table models, database startup, session dependencies, and basic CRUD. Future lessons can build on persistent data, but should continue to distinguish API schemas, database table models, and request-scoped sessions.
