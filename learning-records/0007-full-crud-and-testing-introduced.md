# Full CRUD and API testing introduced

Lessons 0009 and 0010 add PATCH update, DELETE removal, and pytest-based API tests with TestClient and dependency overrides. Future lessons can assume the learner has seen full CRUD and the idea of testing against an isolated database, but should continue reinforcing why tests replace manual `/docs` clicking for regression safety.
