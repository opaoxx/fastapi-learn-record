# First FastAPI API completed

The learner completed the first FastAPI lesson, edited the root endpoint, used PyCharm, and committed the project to GitHub. Future lessons can assume they can run the server locally, inspect `/docs`, edit a route, and use Git for checkpointing.
