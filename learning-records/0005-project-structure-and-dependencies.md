# Project structure and dependency topics introduced

Lessons 0005 and 0006 move the learner from a single-file FastAPI app toward a realistic backend structure with APIRouter modules, shared schemas, a temporary store, and dependency classes. Future lessons can assume the learner has seen route grouping and Depends, but should still reinforce the execution flow before introducing databases or authentication.
