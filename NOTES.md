# Notes

- The learner uses PyCharm and commits lesson progress to GitHub.
- Keep lessons beginner-friendly but tied to backend APIs first, then AI service endpoints and interview-ready projects.
- The learner prefers richer lessons in a classroom / CSDN-blog style: first explain new concepts and underlying principles, then connect them to code and exercises.
- Future lessons should include background, terminology, mechanism, line-by-line code reading, experiments, common errors, and a short quiz.
- From lesson 0027 onward, follow `COURSE-STANDARD.md`: every lesson should be principle-first, mechanism-heavy, code-connected, experiment-driven, and useful for answering interview-style "why" questions.
- The learner explicitly rejected later lessons becoming "code snippets plus brief analysis"; do not trade depth for speed.
- Two-lesson batches are still allowed, but only when both lessons remain substantial. If a topic is dense, prefer one high-quality lesson over two shallow lessons.
- Every lesson and reference page should include top and bottom navigation so readers can return to the course index and move between related pages.
- The learner wants future course updates in two-lesson batches when feasible, while keeping each lesson readable for complete beginners.
- The codebase has now moved from a single-file FastAPI app to a multi-file structure using APIRouter and dependency classes.
- The items endpoints now use SQLite and SQLModel instead of the earlier in-memory dictionary. Keep explaining persistence, sessions, commit, and refresh slowly.
- The items API now has full CRUD plus pytest coverage for create/update/delete and invalid update validation.
- The app now has Settings from pydantic-settings and protects item write endpoints with X-API-Key. Any future POST/PATCH/DELETE examples need the header unless a lesson intentionally demonstrates 401.
- The app now has a protected task API for demo AI-style summary jobs using FastAPI BackgroundTasks and database-backed task status records.
- The app now accepts protected UTF-8 `.txt` uploads and can create summary tasks from uploaded file content.
- The app now has CORS middleware for local browser frontends and serves a small static frontend at `/app/` for uploading a text file and creating a summary task.
- The app now has a small service layer under `first_api/services/`; prediction and summary logic are wrapped by `DemoAIClient`, and `/predict` receives it through `Depends(get_ai_client)`.
- The app now has a structured `AIClientError`; `/predict` converts known AI client failures into 503 responses, while background summary tasks store failures as `status="failed"` with an `error` string.
- `GET /tasks` now supports `status`, `offset`, and `limit` query parameters. The list is ordered by newest task id first, and invalid query values return 422.
- `GET /tasks` now returns a response envelope with `items`, `count`, `limit`, and `offset` instead of a bare list. Any future frontend or test code should read task rows from `items`.
- The static frontend at `/app/` now has a task history section that reads `GET /tasks?limit=5&offset=0`, renders `items`, and displays `count`, `limit`, and `offset` metadata.
- The static frontend task history now includes status filter buttons, a page-size selector, and previous/next pagination controls driven by a shared `historyState` object.
- Lessons 0022 through 0026 have been retrofitted to the new quality standard with deeper principle-first explanations, code reading, common mistakes, experiments, and stronger quick-reference pages.
- Lesson 0029 introduced OpenAPI as the machine-readable API contract behind `/docs`, and the project now has tests that guard the task-list OpenAPI schema and API key security scheme.
- Lesson 0030 introduced schema evolution and backward compatibility. The task-list response envelope now has clearer field metadata, and tests guard both runtime JSON shape and OpenAPI schema shape.
- Lesson 0031 introduced the real AI provider boundary. The app now has an `AIClient` Protocol, `AIClientConfig`, AI provider settings, and tests showing the client remains replaceable.
- Lesson 0032 introduced FastAPI async basics. The app now has `GET /async/wait` as a teaching endpoint that awaits `asyncio.sleep`, with tests for response shape and delay validation.
- Lesson 0033 introduced an async HTTP provider adapter. The app now has `services/provider_http.py`, `ProviderHealthResult`, `check_provider_health()`, and MockTransport tests for success, timeout, and HTTP 503 mapping.
- Lesson 0034 introduced lifespan-managed HTTP clients. The app now creates a provider `httpx.AsyncClient` during lifespan startup, stores it on `app.state`, exposes a teaching state endpoint, and closes the client during shutdown.
- Lesson 0035 introduced the app.state dependency bridge. The app now has `get_provider_http_client()` in `dependencies.py`, the provider HTTP client teaching endpoint receives the client through `Depends`, and tests prove the dependency can be overridden.
- Lesson 0036 introduced a provider health endpoint. The app now has `FIRST_API_AI_PROVIDER_HEALTH_URL`, `GET /provider/health`, `ProviderHealthResponse`, and tests using `MockTransport` plus dependency overrides to verify success and provider 503 mapping without real network access.
- Lesson 0037 introduced the provider prediction adapter. The app now has `ProviderPredictionPayload`, `ProviderPredictionBody`, and `request_provider_prediction()` to send provider JSON, validate provider responses, map invalid responses to `AIClientError`, and return internal `PredictionResult`.
- Lesson 0038 introduced `POST /provider/predict`. The app now has `FIRST_API_AI_PROVIDER_PREDICTION_URL`, an async provider prediction endpoint that returns the stable `PredictionResponse`, and tests that use `MockTransport` plus dependency overrides for success and invalid provider responses.
- Lesson 0039 introduced prediction backend switching. The app now has `FIRST_API_AI_PREDICTION_BACKEND`, and `POST /predict` can choose between the existing demo client and the provider prediction adapter while preserving the stable `PredictionResponse`.
- Lesson 0040 introduced prediction observability. Prediction endpoints now emit `X-Prediction-*` headers and structured logs for backend, source, elapsed time, and stable error codes without changing the JSON response body.
- Lesson 0041 introduced prediction service orchestration. The app now has `services/predictions.py` with `run_prediction()`, `PredictionServiceResult`, and `PredictionServiceError`; routers translate service results into HTTP responses instead of owning backend selection and timing.
- Lesson 0042 introduced provider retry policy. `request_provider_prediction()` now uses `AIClientConfig.max_attempts` to retry timeout, request errors, 429, and common temporary 5xx responses while failing fast for 400-style client errors and invalid provider response bodies.
- Lesson 0043 introduced backoff before retry. The provider config now includes `retry_base_delay_seconds` and `retry_max_delay_seconds`; `request_provider_prediction()` waits with injected async sleep before retrying retryable failures, and tests verify exponential delay calculation without real waiting.
- Lesson 0044 introduced honoring `Retry-After`. Provider retry delay calculation now parses delay-seconds and HTTP-date values, prefers valid provider hints over local exponential backoff, caps them with `retry_max_delay_seconds`, and falls back to local backoff for invalid headers.
- Lesson 0045 introduced retry jitter. Provider retry delay calculation now supports configurable `retry_jitter_seconds`, adds jitter only to local exponential backoff, leaves valid `Retry-After` hints unjittered, and injects the random fraction function so tests remain deterministic.
- Lesson 0046 introduced retry observability. Provider retry waits now create a `RetryDelayPlan`, log `provider_prediction_retry_scheduled` before sleeping, and expose structured fields such as attempt, retry reason, status code, Retry-After, delay seconds, and delay source; tests use `caplog` to verify these diagnostic fields.
- Lesson 0047 introduced retry exhaustion. Provider prediction retries now log `provider_prediction_retry_exhausted` at warning level when timeout, request error, or retryable HTTP status failures use all allowed attempts; non-retryable HTTP errors and invalid provider responses still fail fast without being mislabeled as exhausted retries.
- Lesson 0048 introduced fail-fast observability. Non-retryable provider HTTP status errors and invalid provider prediction response bodies now log `provider_prediction_fail_fast` with attempt, max attempts, fail-fast reason, status code, and stable error code; tests verify these paths do not retry and do not emit retry-exhausted logs.
- Lesson 0049 introduced provider error taxonomy. Provider HTTP status classification now has a stable `classify_provider_http_status()` helper returning `retryable_http_status` or `non_retryable_http_status`, and tests protect the taxonomy names used by lessons, logs, and retry/fail-fast reasoning.
- Lesson 0050 introduced provider metrics basics. Provider prediction metric labels now have a stable `ProviderMetricLabels` dataclass and `build_provider_metric_labels()` helper, keeping outcome, failure category, error code, and status code low-cardinality and excluding user request text from metric labels.
- Lesson 0051 introduced provider metrics counters. A teaching-only `ProviderMetricsCounter` now shows how identical provider metric label sets accumulate counts and how snapshots expose labels plus count, with tests for accumulation and positive-only increments.
- Lesson 0052 introduced a provider metrics endpoint. Provider prediction outcomes now increment the teaching counter for success, retry scheduled, retry exhausted, and fail-fast events; `GET /provider/metrics` exposes the current in-process snapshot with a response model and tests for endpoint shape.
- Lesson 0053 introduced provider metrics failure outcomes. Endpoint-level tests now prove `GET /provider/metrics` exposes retry failure samples for retryable HTTP 503 paths and fail-fast samples for non-retryable HTTP 400 paths.
- Lesson 0054 introduced provider metrics test isolation. `tests/test_ai_client_dependency.py` now uses an autouse pytest fixture to reset the in-process provider metrics counter before and after every test in that file, keeping metrics endpoint tests order-independent.
- Lesson 0055 introduced provider metrics export shape. The app now renders the existing provider metrics snapshot into a Prometheus text-style teaching format and exposes it at `GET /provider/metrics/prometheus` with `PlainTextResponse`.
- Lesson 0056 introduced provider metrics export tests. Text export coverage now protects empty snapshots, retry failure outcome lines, special label value escaping, and the `text/plain` endpoint boundary.
- Lesson 0057 introduced provider metrics snapshot boundary. `ProviderMetricsCounter.snapshot()` is now protected by a test proving callers cannot mutate returned snapshot data to alter the counter's internal state.
- Lesson 0058 introduced provider metrics production boundary. OpenAPI contract tests now protect the metrics endpoint descriptions that mark the in-process counter and text export as educational, not a production multi-instance metrics pipeline.
- Lesson 0059 introduced provider metrics naming contract. Tests now protect `provider_prediction_total`, its HELP text, and the stable low-cardinality label keys used by the provider prediction metrics export.
- Lesson 0060 introduced provider metrics naming refactor boundary. The provider HTTP service now exposes a `get_provider_prediction_metric_contract()` helper and label-name tuple so metric rename-sensitive fields are visible and covered by tests before any future migration.
- Lesson 0061 introduced a provider metrics documentation index. The course now has `reference/provider-metrics-index.html` as a stable entry point for provider metrics endpoints, metric contracts, production boundaries, related lessons, and tests, with `tests/test_course_docs_contract.py` protecting the required documentation facts and links.
- Lesson 0062 introduced a provider metrics runbook checklist. The course now has `reference/provider-metrics-runbook.html` for investigating provider metrics by endpoint, outcome, failure category, error code, and status code, with documentation tests protecting the runbook's required signals and links.
- The learner paused new lesson production after Lesson 0062 and requested a full quality review/refactor of the first 62 lessons into a "university CS final-review notes + top CSDN technical article" style: dense, principle-first, code-bound, interview-ready, no shallow online-course narration.
- Refactor batch 1 completed Lessons 0001-0006. These foundation lessons now use the required nine-section structure: knowledge framework, underlying principles, line-by-line code reading, runtime flow, confusing-point comparisons, common errors, production-vs-teaching differences, review exercises with answers, and interview questions with analysis.
- Refactor batch 2 completed Lessons 0007-0012. These lessons now cover SQLite/SQLModel persistence, Session CRUD, PATCH/DELETE semantics, TestClient isolation, Settings/environment-variable boundaries, and API Key authentication in the same high-density nine-section review style.
- Refactor batch 3 completed Lessons 0013-0018. These lessons now cover BackgroundTasks, task status envelopes, UploadFile text uploads, file-backed summary tasks, CORS/browser preflight mechanics, and the static frontend fetch workflow in the same high-density nine-section review style.
- Refactor batch 4 completed Lessons 0019-0024. These lessons now cover service-layer boundaries, AI client dependency inversion, stable AI error contracts, async task failure state, task-list pagination, and status filtering in the same high-density nine-section review style.
- Refactor batch 5 completed Lessons 0025-0030. These lessons now cover list response envelopes, count queries, frontend task-history projection, frontend pagination/status controls, OpenAPI contracts, and schema evolution/backward compatibility in the same high-density nine-section review style.
- Refactor batch 6 completed Lessons 0031-0036. These lessons now cover real AI provider boundaries, FastAPI async basics, async HTTP provider adapters, lifespan-managed HTTP clients, app.state dependency bridges, and provider health endpoints in the same high-density nine-section review style.
- A documentation contract test now protects the rebuilt foundation lessons 0001-0036 from losing the nine-section review structure.
